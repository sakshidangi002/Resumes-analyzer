# Dependencies for the unit-test suite in tests/.
#
# Deliberately NOT the full backend requirements: importing anything under
# app/api/routes pulls in opencv, torch, insightface and ultralytics (~GBs),
# which would make CI slow and brittle for tests that exercise pure logic.
#
# Tests that genuinely need the vision stack call pytest.importorskip and are
# reported as SKIPPED (CI runs pytest with -rs so that is visible, never silent).
# To run those locally, install Attendance Management/backend/requirements.txt.
pytest>=8.0
ruff>=0.5

# app.core.config / encrypted_types / security / net
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
fastapi>=0.110
sqlalchemy>=2.0
bcrypt>=4.0
cryptography>=42.0
python-jose[cryptography]>=3.3
